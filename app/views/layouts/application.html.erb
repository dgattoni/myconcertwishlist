<!DOCTYPE html>
<html>
<head>
  <title>My Concerts Wishlist - by Daniela Gattoni</title>
    <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
 <!-- arbol cargado desde assets/stylesheets -->
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
 <!-- arbol cargado desde assets/javascripts-->
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>  
  <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script> 
  <link href='http://fonts.googleapis.com/css?family=Lobster+Two:400,400italic' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>
</head>
<body>

<%= render 'layouts/navbar' %>
<%= yield %>


<!-- agregamos color aleatorio a los div que contengan la clase .item
  si quieres quitar este efecto simplemente borra esta porcion de code Jquery -->
  <script type="text/javascript">
    $('.item').each(function () {
      var hue = 'rgba(' + (Math.floor((256)*Math.random()) + 1) + ',' + (Math.floor((256)*Math.random()) + 2) + ',' + (Math.floor((256)*Math.random()) + 9) + ',1)';
      $(this).css("background-color", hue);
    });

    $(".item").click(function() {
      var estilo = $(this).attr( "style");
      $( "#resultadoCss" ).html(estilo);
    });

  </script>


</body>
</html>
